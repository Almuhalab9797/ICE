/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { WhichTeamComponent } from './which-team.component';

describe('WhichTeamComponent', () => {
  let component: WhichTeamComponent;
  let fixture: ComponentFixture<WhichTeamComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ WhichTeamComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(WhichTeamComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});*/
